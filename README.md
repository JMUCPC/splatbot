# splatbot

## Development Setup

This project uses Python 3.12. This is not the latest version, as we wait for updates to NiceGUI.
```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```